name: flutter_architecture_example
description: A Flutter demo for using MVVM architecture and services.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider_architecture: ^1.0.5
  shared_preferences: ^0.5.6
  sqflite: ^1.2.0
  path_provider: ^1.5.1
  http: ^0.12.0+4
  get_it: ^3.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1


flutter:
  uses-material-design: true
